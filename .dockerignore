# Dependencies — reinstalled inside the build stage
node_modules

# Build output — produced fresh inside the build stage
dist
dist-ssr

# Version control
.git
.gitignore

# Environment / secrets — must not land in the image
.env
.env.*
!.env.example

# Editor / OS noise
.vscode
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Docs (not needed to build the app)
README.md
